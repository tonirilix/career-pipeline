{
  "name": "career-pipeline",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "test": "npm run test --workspaces",
    "dev": "npm run dev --workspace apps/web"
  },
  "msw": {
    "workerDirectory": [
      "apps/web/public"
    ]
  }
}
