# Override the GraphQL endpoint URL.
# Defaults to http://localhost:8080/graphql when not set.
# VITE_API_URL=http://localhost:8080

# Scripts:
#   npm run dev        → DEV=true,  MSW starts (mock data)
#   npm run dev:api    → DEV=false, MSW skipped, hits real Go backend on VITE_API_URL
