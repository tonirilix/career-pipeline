mutation UpdateRoleDecision(
  $id: ID!
  $status: String!
  $rejectionReason: String
) {
  updateRoleDecision(
    id: $id
    status: $status
    rejectionReason: $rejectionReason
  ) {
    ...RoleRecordFields
  }
}
